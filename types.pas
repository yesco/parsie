record name:place; loc:record x:integer; y:real; end; end

array[0..31] of integer

array[1..26] of record x:integer; y:string; end

# TODO: propagate Z_size from each member adding to Z_size for record
# just init each type object from name get global object
record proto:real; reserved:real; n:real; next:real; props:array[6] of record name:real; val:real; end; end

array[3..7] of 10..100
   
